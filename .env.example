# Non-secret defaults, committed so local builds have a sane fallback.
# The GitHub Pages deploy workflow overrides VITE_SITE_URL with the real,
# current Pages URL (see .github/workflows/deploy.yml) — update this only
# to keep local `npm run build` output realistic.
VITE_SITE_URL=https://xenthrall.github.io/tejidos-yolis
